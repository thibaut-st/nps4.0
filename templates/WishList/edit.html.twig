{% extends 'base.html.twig' %}

{% block body %}
    <h1>{% trans %}list.title.edit{% endtrans %}</h1>
    <hr>
    <div class="row">
        <div class="col-lg-4">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <a class="btn btn-cancel" href={{ path('wishlist_show', {'id': id}) }}>{% trans %}cancel{% endtrans %}</a>
            <button type="submit" class="btn btn-success">{% trans %}edit{% endtrans %}</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}

